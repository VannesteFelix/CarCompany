#pragma once

//	CLASS ENUM 
// to add easily new possible brand/car type/option

enum class VehiculeBrand { renault, citroen, toyota };
enum class CarType { decapotable, coupe, sedan };
enum class PossibleOption { fenetre, porte, roue, volant, moteur };
